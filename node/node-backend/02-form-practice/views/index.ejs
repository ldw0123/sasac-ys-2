<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GET & POST로 정보 받기 실습</title>
  </head>
  <body>
    <h1>GET & POST로 정보 받기 실습</h1>

    <!-- ----------- post 요청 ----------- -->
    <form action="/post" method="post" target="_black" name="login">
      이름 <input type="text" name="name" />

      <fieldset>
        <legend>성별</legend>
        <!-- name이 같으면 하나만 선택, 다르면 여러 개 선택 가능 -->
        <input type="radio" name="gender" value="남자" id="men" /> <label for="men">남자</label>
        <input type="radio" name="gender" value="여자" id="women" /> <label for="women">여자</label>
      </fieldset><br />

      <fieldset>
        <legend>생년월일</legend>
        <select type="select">
          <% for (let i = 1970; i < 2021; i ++) { %>
          <option value="<%= i %>"> <%= i %> </option>
          <% } %>
        </select>년

        <select type="select">
          <% for (let i = 1; i < 13; i ++) { %>
          <option value="<%= i %>"> <%= i %> </option>
          <% } %>
        </select>월

        <select type="select">
          <% for (let i = 1; i < 32; i ++) { %>
          <option value="<%= i %>"> <%= i %> </option>
          <% } %>
        </select>일
      </fieldset><br />

      <fieldset>
        <legend>관심사</legend>
        <input type="checkbox" name="trip" value="여행" id="trip" /> <label for="trip">여행</label>
        <input type="checkbox" name="fashion" value="패션" id="fashion" /> <label for="fashion">패션</label>
        <input type="checkbox" name="food" value="음식" id="food" /> <label for="food">음식</label>
      </fieldset><br />

      <fieldset>
        <legend>선택</legend>
        좋아하는 색상 <input type="color" /><br />
        좋아하는 숫자 <input type="number" />
      </fieldset><br />

      <input type="tel" name="num" placeholder="전화번호" pattern="^[0-9]{3}-[0-9]{4}-[0-9]{4}$" maxlength="13" title="010-0000-0000 형식" required />
      <input type="id" name="id" placeholder="아이디" pattern="^([a-z0-9]){5,}$" title="영어 소문자, 숫자만 사용 가능 (5글자 이상)" required />
      <input type="password" name="password" placeholder="비밀번호" pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[!@#\$%^&*])[a-zA-Z0-9!@#\$%^&*]{8,}$" title="영어 소문자, 대문자, 숫자, 특수 문자를 모두 한 번 이상은 사용 (8글자 이상)" required /><br />
      <br /><button type="submit">회원가입</button>

      <!-- 정규식 -->
      <!-- ^ : 문자열의 시작을 나타냅니다. -->
      <!-- (?=.*[a-z]) : 적어도 하나의 소문자 알파벳이 필요합니다. -->
      <!-- (?=.*[A-Z]) : 적어도 하나의 대문자 알파벳이 필요합니다. -->
      <!-- (?=.*\d) : 적어도 하나의 숫자가 필요합니다. -->
      <!-- (?=.*[!@#\$%^&*]) : 적어도 하나의 특수 문자 (여기서는 !, @, #, $, %, ^, &, *)가 필요합니다. -->
      <!-- [a-zA-Z0-9!@#\$%^&*]{8,} : 허용된 문자 집합(영어 대문자, 소문자, 숫자, 특수 문자) 중 8개 이상의 문자를 허용합니다. -->
      <!-- $ : 문자열의 끝을 나타냅니다.  -->
    </form>

  </body>
</html>